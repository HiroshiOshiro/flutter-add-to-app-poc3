.gradle/
build/
*.iml
.idea/
local.properties
.DS_Store

# CIのキャッシュ置き場。cache:paths がプロジェクト外を指定できないため、
# pub / Gradle のキャッシュをここへ寄せている（.gitlab-ci.yml）。
.cache/

# iOS
legacy_ios/build/
*.xcuserstate
xcuserdata/

# Flutter module (生成物。flutter pub get で再生成される)
legacyapp_flutter/.android/
legacyapp_flutter/.ios/
legacyapp_flutter/build/
legacyapp_flutter/.dart_tool/
legacyapp_flutter/.flutter-plugins-dependencies

# Xcode
legacy_ios/build/
